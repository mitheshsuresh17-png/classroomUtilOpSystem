# CLUS Database Configuration
# Copy this file to .env and fill in your actual values.
# NEVER commit the .env file to version control.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=classroom_utilization_db
PORT=5000
